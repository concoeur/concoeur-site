---
const { title = "CONCOEUR" } = Astro.props;
---
<html lang="sv">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content="CONCOEUR. " />
    <style is:global>


/* Gray green blue for header links */
.menu a{
  color: #67a88e;          /* gray green blue */
  text-decoration: none;
}

.menu a:hover{
  color: #7ac4a7;          /* lite ljusare vid hover */
  text-decoration: none;
}

.manifesto {
  max-width: 70ch;
  margin: 0 auto;
  text-align: center;
  font-style: italic;
  color: #777;
  font-size: 20px;
  line-height: 1.7;
}

/* Startsida */
.hero{
  min-height: calc(100vh - 120px);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 72px 20px 40px;
}
.manifest{
  margin: 0;
  max-width: 58ch;
  text-align: center;
  font-style: italic;
  color: #777;
  line-height: 1.8;
  font-size: 18px;
}

/* Header – bättre klickytor */
.menu a{
  display: inline-block;
  padding: 10px 6px;
  margin-left: 10px;
  font-style: italic;
  text-decoration: none;
}

/* Mer luft mellan header och sidinnehåll */
.wrap{
  padding: 88px 20px 72px;
}

/* Mobil */
@media (max-width: 820px){
  .nav {
    flex-direction: column;
    gap: 10px;
    align-items: center;
    text-align: center;
  }
  .brand {
    text-align: center;
  }
  .menu {
    text-align: center;
  }
  .menu a{
    margin: 0 10px;
    padding: 10px 6px;
  }
}

  }
}

}


      body { margin:0; font-family:"Times New Roman", Times, serif; background:#fbfaf7; color:#111; }
      .wrap { max-width: 980px; margin: 0 auto; padding: 64px 20px 72px; }
      header { position: sticky; top: 0; background: rgba(251,250,247,0.9); backdrop-filter: blur(8px); border-bottom:1px solid #ddd; }
      .nav { max-width:980px; margin:0 auto; padding:12px 20px; display:flex; justify-content:space-between; align-items:center; }
      .brand { letter-spacing:.04em; font-size:16px; text-decoration:none; color:#111; }
.brand em { font-style: italic; opacity:.7; margin-right:4px; }
.brand-strong { font-style: normal; letter-spacing:.08em; }
.brand:hover { text-decoration: none; }

      .menu a { margin-left:14px; font-style: italic; text-decoration:none; opacity: 0.7 }
      .menu a:hover { opacity: 1; }
      h1 { font-size: 44px; letter-spacing:.04em; margin: 0 0 107px; text-align: center }
      p { color:#555; line-height:1.6; max-width:70ch; }
      footer { border-top:1px solid #ddd; margin-top:48px; padding-top:18px; color:#555; font-size:13px; }
      @media (max-width: 820px){
        .nav { flex-direction: column; gap: 8px; align-items:flex-start; }
        h1 { font-size: 36px; }
      }

.lang {
  font-style: normal;
  letter-spacing: .08em;
  font-size: 12px;
  opacity: .6;
}
.lang:hover {
  opacity: 1;
  text-decoration: none;
}

.bottom-space{
  height: 977px;
}

@media (max-width: 820px){
  .bottom-space{
    height: 120px;
  }
}

/* Force mobile centering of header */
@media (max-width: 900px){
  header .nav{
    flex-direction: column !important;
    align-items: center !important;
    text-align: center !important;
  }
  header .brand{
    text-align: center !important;
    display: block;
  }
  header .menu{
    text-align: center !important;
  }
  header .menu a{
    margin: 0 10px !important;
    padding: 12px 8px !important;
    display: inline-block;
  }
}


    </style>
  </head>
  <body>
    <header>
      <div class="nav">
        <a class="brand" href="/">
  <em>hyperbaric oxygen </em> <span class="brand-strong">by CONCOEUR</span>
</a>
      <nav class="menu">
  <a href="/boka">bokning</a>
  <a href="/kontakt">kontakt</a>
  <a href="/integritet">integritet</a>

</nav>


        </nav>
      </div>
    </header>

  <main class="wrap">
  <slot />
  <div class="bottom-space"></div>
  <footer>© CONCOEUR AB</footer>
</main>


  </body>
</html>
